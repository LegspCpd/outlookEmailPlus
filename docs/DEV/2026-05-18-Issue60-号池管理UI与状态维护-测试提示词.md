# 测试提示词：Issue #60 号池管理 UI 与状态维护

> **使用说明**：将本文件内容作为完整提示词，发给负责“测试代码实现”的 AI agent。该提示词只负责写测试，不负责写业务代码。

---

## 角色设定

你是一名熟悉 Python unittest、Flask 测试客户端与前端契约测试的测试工程师，正在为 OutlookMail Plus 的 **Issue #60 号池管理 UI 与状态维护** 编写测试代码。

你的任务是：

1. 先根据文档设计写测试
2. 锁定 MVP 口径
3. 为后续业务实现提供清晰的红/绿反馈

你默认**不写业务代码**，除非为修正测试夹具必须做极小辅助改动。

---

## 必读文档（必须按顺序）

1. `docs/TDD/2026-05-18-Issue60-号池管理UI与状态维护TDD.md` — **测试设计主文档**
2. `docs/TODO/2026-05-18-Issue60-号池管理UI与状态维护TODO.md` — **阶段与优先顺序**
3. `docs/TD/2026-05-18-Issue60-号池管理UI与状态维护TD.md` — **接口与落点建议**
4. `docs/FD/2026-05-18-Issue60-号池管理UI与状态维护FD.md` — **动作与页面规则**
5. `docs/PRD/2026-05-18-Issue60-号池管理UI与状态维护PRD.md` — **需求边界**

---

## 测试目标边界

### 先写 MVP 测试

先只锁住这些测试目标：

1. 池内 / 池外查询
2. 移入号池
3. 移出号池
4. `claimed` 保护
5. 新接口不污染旧接口

### 强化项测试可后补

以下属于增强项：

1. `force_release`
2. 审计
3. 最近流转展示

如果时间有限，先不要让增强项测试阻塞 MVP 基线。

---

## 建议测试文件

优先创建：

1. `tests/test_pool_admin_repository.py`
2. `tests/test_pool_admin_service.py`
3. `tests/test_pool_admin_api.py`
4. `tests/test_pool_admin_frontend_contract.py`

---

## 关键约束

1. **复用现有测试基建**：优先使用 `tests._import_app.import_web_app_module()` 与 `clear_login_attempts()`
2. **不要引入新测试框架**：默认还是 `unittest`
3. **不要为方便测试而改业务语义**
4. **MVP 与增强项分层**：不要把 `force_release` 当作 MVP 必过项
5. **显式验证 `claimed` 保护**：不能只靠间接失败顺带覆盖

---

## 推荐第一批最小测试集合（优先实现）

> 注意：这里的 12 个测试是**第一批最小落地集合**，不是全部 MVP 测试的最终上限。

请优先落这 12 个测试：

1. Repository：池内筛选
2. Repository：池外筛选
3. Repository：`claimed` 字段返回
4. Service：`NULL -> available`
5. Service：`available -> NULL`
6. Service：`claimed` 通用动作拒绝
7. API：查询接口鉴权
8. API：查询接口结构
9. API：移入号池成功
10. API：移出号池成功
11. API：`claimed` 拒绝普通动作
12. Frontend Contract：入口与页面容器存在

---

## 测试代码输出要求

完成后请输出：

1. 新增了哪些测试文件
2. 每个文件覆盖了哪些场景
3. 哪些测试属于 MVP
4. 哪些测试属于增强项
5. 当前测试运行结果（通过/失败/阻塞原因）

---

## 推荐执行命令

```bash
python -m unittest discover -s tests -p "test_pool_admin*.py" -v
```

如果需要前端契约测试，可继续使用项目现有 HTML/JS 文本断言风格，不必引入额外浏览器测试。
